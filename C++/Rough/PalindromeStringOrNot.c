#include <stdio.h>
#include <string.h>

int main(){
    char str1[50], str2[50], length;
    printf("Enter a string: ");
    gets(str1);
    length = strlen(str1);

    for (int i = 0; i <= length; i++)
    {
        
    }

}