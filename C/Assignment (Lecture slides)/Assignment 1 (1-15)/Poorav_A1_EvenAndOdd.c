#include <stdio.h>

int main(){
    int a, x;
    printf("Enter a number\n");
    scanf("%d", &a);
    x + a % 2;
    if (x == 0)
    printf("Number a = %d is even \n", a);
    else
    printf("Number a = %d is odd \n", b);
    return 0;
}